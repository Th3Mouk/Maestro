apiVersion: maestro/v1
kind: Workspace
metadata:
  name: collision-workspace
spec:
  framework:
    version: ^1.0.0
  runtimes:
    standard:
      enabled: true
    claude-code:
      enabled: true
  agents:
    standard:
      - planner
    claude-code:
      - planner
