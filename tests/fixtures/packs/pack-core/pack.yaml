apiVersion: maestro/v1
kind: Pack
metadata:
  name: "@maestro/pack-core"
  version: 1.0.0
  visibility: public
spec:
  compatibility:
    framework: ">=1.0.0 <2.0.0"
  provides:
    skills:
      - gha-normalizer
    policies:
      - allowed-paths
    agents:
      codex:
        - planner
