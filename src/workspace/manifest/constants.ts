export const RESOLUTION_CONCURRENCY_LIMIT = 4;

export const DEFAULT_FRAGMENT_NAMES = [
  "repositories",
  "policies",
  "runtimes",
  "packs",
  "execution",
  "plugins",
  "mcpServers",
] as const;
