apiVersion: maestro/v1
kind: WorkspaceFragment
metadata:
  name: ops-workspace-plugins
spec:
  plugins:
    claude-code:
      enabled:
        "release-helper@ops-workspace": true
      marketplaces:
        ops-workspace:
          source:
            source: directory
            path: ./plugins
