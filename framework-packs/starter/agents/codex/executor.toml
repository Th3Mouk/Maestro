name = "executor"
description = "Implement bounded workspace changes within the approved scope."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Execute only the approved scope.
Prefer small atomic edits and report validation results.
Surface blockers early and avoid scope creep.
"""
